"use strict"; // treat all JS code as newer version

// alert(3+3) // we are using node JS, not browser
console.log(3+3); console.log("Anik"); // not readable code
// code readability should be high


let name = "Anik" // string datatype
let age = 22 // number datatype
let isLoggedIn = false // boolean datatype to check whether user has logged in or not
/* Primitive Datatypes:
number => range: 2^53
bigint => if the number is very large especially in trading, stock market, reddit, facebook, etc
string => preferred to use ""
boolean => true/false
null => standalone value
undefined => value is not assigned till now
symbol => used in React to find uniqueness */
// Object
console.log(typeof "Anik"); // o/p => string
console.log(typeof age); // o/p => number
console.log(typeof null); // o/p => object
console.log(typeof undefined); //o/p => undefined

